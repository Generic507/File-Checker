This program serves as baseline file checker made to understand and apply modern hasing algorithms to identify data integrity.

The filechecker python file creates a baseline.json file storing the calculated hash of the three individual files in the respository.
